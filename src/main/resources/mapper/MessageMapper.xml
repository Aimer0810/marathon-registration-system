<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marathon.mapper.MessageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (user_id, content) VALUES (#{userId}, #{content})
    </insert>

    <update id="updateReply">
        UPDATE message SET reply = #{reply} WHERE id = #{id}
    </update>

    <select id="selectByUserId" resultType="com.marathon.entity.Message">
        SELECT * FROM message WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultType="com.marathon.entity.Message">
        SELECT m.*, u.username AS user_name
        FROM message m
                 LEFT JOIN user u ON m.user_id = u.id
        ORDER BY m.create_time DESC
    </select>
</mapper>
