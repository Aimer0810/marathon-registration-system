<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.marathon.mapper.AiChatMessageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_message (user_id, event_id, question, answer)
        VALUES (#{userId}, #{eventId}, #{question}, #{answer})
    </insert>

    <select id="selectByUserId" resultType="com.marathon.entity.AiChatMessage">
        SELECT *
        FROM ai_chat_message
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>
</mapper>
